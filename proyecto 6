#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int i=-15;
	for(i=-15;i<40;i+5){
		i=i+5;
		sleep(1);
	   printf(" %d ", i);
	   
	}
		
	getch();
	
	return 0;//programa completado.
}
